scalar Contact
scalar Email

enum Role {
  admin
  user
}

type Query {
  users(
    id: ID
    username: String
    contact: Contact
    email: Email
    role: Role
  ): [User!]!
}

type User {
  id: ID!
  username: String!
  contact: Contact!	
  email: Email!
  role: Role!
}
