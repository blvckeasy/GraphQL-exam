type Query {
  categories(
    id: ID
    name: String
  ): [Category!]!
}

type Mutation {
  addCategory(
    token: String!
    category_name: String!
  ): Category!
}

type Category {
  id: ID!
  name: String!  
}
